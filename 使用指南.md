# 断层多边形自动追踪 — 使用指南

东方杯赛题7 · Streamlit 交互式可视化应用

## 快速开始

### 1. 安装依赖
```bash
pip install -r requirements.txt
```

### 2. 启动应用
双击 `run_app.bat`，或命令行：
```bash
streamlit run app/main.py --server.port 8501
```
浏览器自动打开 `http://localhost:8501`。

### 3. 使用流程
1. **左侧面板** → 点击「生成合成数据」或上传 .npy/.npz 文件
2. 展开参数面板调整算法参数
3. 点击「运行断层追踪」
4. 切换到上方页面标签查看结果

## 四个页面

| 页面 | 功能 | 什么时候用 |
|------|------|-----------|
| 首页 | 数据加载 + 结果预览 | 每次启动 |
| 分步流水线 | 7步可视化 + AI语音讲解 | 答辩演示 |
| 参数调节 | 滑块调参 + 自动刷新 | 调参优化 |
| 交互探索 | 全屏地图 + 点击多边形详情 | 数据分析 |
| 结果对比 | 双参数集并排对比 | 效果对比 |

## 项目结构

```
├── app/                    # Streamlit 前端应用
│   ├── main.py             # 入口，侧边栏 + 首页
│   ├── pages/              # 4个功能页面
│   ├── components/         # 可复用 UI 组件
│   ├── viz/                # Plotly 图表生成
│   ├── pipeline/           # 桥接层（不修改 src/）
│   └── assets/             # 深色主题 CSS
├── src/                    # 算法模块（不变）
│   ├── preprocess.py       # 预处理
│   ├── segment.py          # 断层区域分割
│   ├── polygon_extract.py  # 多边形提取
│   ├── vectorize.py        # 矢量化 + 导出
│   ├── visualize.py        # matplotlib 可视化
│   └── utils.py            # 工具函数
├── config.py               # 可调参数类
├── main.py                 # CLI 入口（旧，保留）
├── demo.py                 # PPT 演示脚本（旧，保留）
├── run_app.bat             # 一键启动
└── requirements.txt        # 依赖
```

## 给队友的开发说明

- **Python 环境**：项目用 `D:/my anaconda/python.exe`（Python 3.12）
- **不要改 `src/`**：算法核心模块已稳定，修改参数用 `config.py`
- **前端在 `app/`**：所有 Streamlit 代码在这里
  - 加新页面：在 `app/pages/` 放 `.py` 文件，Streamlit 自动识别
  - 改样式：编辑 `app/assets/custom.css`
  - 调图表：`app/viz/plotly_figs.py`
- **运行测试**：`python main.py` 跑 CLI 版本，验证算法是否正常
