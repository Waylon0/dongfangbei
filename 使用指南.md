# 断层多边形自动追踪 — 使用指南

## 项目简介

这是"东方杯"第十二届勘探地球物理软件开发大赛**赛题7**的参赛项目。程序输入一张断层属性图（类似医学 CT），自动找出所有断层区域，并把每个断层用**闭合多边形**圈出来。

---

## 一、你拿到的是什么

一个压缩包 `fault_tracker.zip`，解压后文件结构：

```
fault_tracker/
├── main.py               ← 主程序入口，运行这个
├── config.py             ← 所有可调参数在这里
├── requirements.txt      ← Python 依赖列表
├── 使用指南.md            ← 你正在看的这份文档
├── src/
│   ├── preprocess.py     ← 数据预处理（归一化、去噪）
│   ├── segment.py        ← 断层区域分割（阈值+形态学）
│   ├── polygon_extract.py ← 多边形轮廓提取
│   ├── vectorize.py      ← 多边形简化+平滑+导出
│   ├── visualize.py      ← 结果可视化
│   └── utils.py          ← 公共工具函数
└── data/                 ← 运行后自动创建，存放输出文件
```

---

## 二、第一次运行（3 步）

### 第 1 步：安装 Python 依赖

打开终端（cmd 或 PowerShell），进入解压目录，执行：

```bash
pip install -r requirements.txt
```

需要安装的库只有 5 个：`numpy` `scipy` `scikit-image` `shapely` `matplotlib`

### 第 2 步：运行

```bash
python main.py
```

### 第 3 步：查看结果

程序运行约 0.1 秒后，在 `data/` 目录生成以下文件：

```
data/
├── fault_polygons.geojson  ← 断层多边形（GeoJSON，可用 GIS 软件打开）
├── fault_polygons.txt      ← 断层多边形（纯文本，GeoEast 兼容格式）
├── intermediate.png         ← 中间过程图（原始属性 vs 分割结果）
├── final_result.png         ← 最终结果图（彩色多边形叠加在属性图上）
└── polygon_stats.png        ← 面积统计图
```

终端输出示例：

```
--- Results ---
  Elapsed: 0.126s
  Binary mask pixels: 25016
  Raw contours: 8
  Vectorized polygons: 8
  After area filter: 8
  Areas: min=471.3, max=1188.9, mean=739.7, median=714.6
```

---

## 三、如何调参数

所有参数都在 `config.py` 文件里，改了保存后重新运行 `python main.py` 即可。

| 参数 | 含义 | 默认值 | 调参建议 |
|------|------|:---:|------|
| `gaussian_sigma` | 去噪强度 | 1.5 | 数据噪声大就调大 |
| `otsu_scale` | 阈值灵敏度 | 1.0 | 想检测更多断层就 < 1.0，想更保守就 > 1.0 |
| `closing_radius` | 断缝填充半径 | 5 | 断层线有断裂就调大 |
| `opening_radius` | 噪点去除半径 | 2 | 结果里噪点多就调大 |
| `min_polygon_area` | 最小面积过滤 | 50 | 小于此值的多边形直接丢弃 |
| `dp_epsilon` | 轮廓简化程度 | 3.0 | 值越小轮廓越精细（但点多）、越大越粗糙 |
| `smooth_iterations` | 平滑次数 | 2 | 边界不够光滑就调大 |

---

## 四、项目是如何工作的（流水线详解）

整个算法分 **6 个阶段**，就像一个工厂流水线：

```
 原始属性图（2D 数值矩阵）
      │
      ▼
┌─────────────────────────────┐
│ 阶段1：预处理                │
│ 高斯滤波去噪 → 归一化        │  preprocess.py
└─────────────────────────────┘
      │
      ▼
┌─────────────────────────────┐
│ 阶段2：断层区域分割          │
│ Otsu二值化 → 形态学闭运算    │  segment.py
│ (填小孔) → 开运算(去噪点)    │
│ 输出：黑白掩膜（白=断层）    │
└─────────────────────────────┘
      │
      ▼
┌─────────────────────────────┐
│ 阶段3：多边形轮廓提取        │
│ 连通域分析 → 骨架化交叉分离  │  polygon_extract.py
│ → Marching Squares 轮廓追踪  │
│ 输出：原始轮廓坐标            │
└─────────────────────────────┘
      │
      ▼
┌─────────────────────────────┐
│ 阶段4：矢量简化+平滑         │
│ Douglas-Peucker 折线简化    │  vectorize.py
│ → Chaikin 角点切割平滑       │
│ 输出：简化后的多边形坐标     │
└─────────────────────────────┘
      │
      ▼
┌─────────────────────────────┐
│ 阶段5：面积过滤              │
│ Shoelace 公式计算面积       │  vectorize.py
│ → 丢弃小于阈值的小多边形     │
└─────────────────────────────┘
      │
      ▼
┌─────────────────────────────┐
│ 阶段6：导出+可视化           │
│ GeoJSON / TXT / PNG         │  visualize.py
└─────────────────────────────┘
```

### 核心算法要点

**为什么用形态学闭运算？** 断层在属性图上可能不连续（有断缝），闭运算能把断开的区域连起来，形成完整闭合的多边形。

**如何处理交叉断层？** 两个断层相交时，在二值图上会连成一个区域。骨架化后找到交叉点（度数≥3的节点），在交叉点处切开，再分别膨胀恢复，就能把连在一起的断层分开了。

**如何确保多边形闭合？** 用 Marching Squares 算法沿区域边界追踪，首尾自动相连。导出前会检查——如果首尾距离超过 2 像素，自动补一条闭合边。

---

## 五、用真实数据替换合成数据

当前 `main.py` 里 `generate_synthetic_data()` 是生成假数据的函数。拿到组委会的真实数据后，修改 `main.py` 的 `main()` 函数：

```python
def main():
    # 原来：生成合成数据
    # data = generate_synthetic_data(...)

    # 改为：加载真实数据
    data = np.load('real_data.npy')  # 或从 GeoEast 数据库读取

    # 其余代码不变
    cfg = Config()
    result = run_pipeline(data, cfg)
    ...
```

如果你的数据是 SEG-Y 格式，可以用 `obspy` 或 `segpy` 库读取。如果是 GeoEast 数据库，需要组委会提供 API。

---

## 六、常见问题

**Q: 运行报 `ModuleNotFoundError`？**
A: 没有装依赖，先执行 `pip install -r requirements.txt`。

**Q: 发现的多边形太多/太少？**
A: 调整 `config.py` 里的 `otsu_scale`（越大越保守）和 `closing_radius`（越大连接越多）。

**Q: 多边形边界不平滑？**
A: 增大 `smooth_iterations`（平滑次数）或减小 `dp_epsilon`（保留更多细节）。

**Q: 想自己生成不同样式的测试数据？**
A: 修改 `main.py` 里 `generate_synthetic_data()` 的参数：`n_faults`（断层数量）、`noise_level`（噪声水平）、`seed`（随机种子）。
